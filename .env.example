# Stage 1 has no required server-side secrets.
# Do not store model API keys here. Model API keys will be stored in browser IndexedDB in later stages.

PORT=8000
TEMP_DIR=/tmp/bilibili-transcription
GLOBAL_TASK_CONCURRENCY=1
AUDIO_REQUEST_CONCURRENCY=2
