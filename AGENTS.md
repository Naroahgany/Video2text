# AGENTS.md

本文档定义本项目中所有开发代理必须遵守的协作规则。除非用户明确更新本文件，否则这些规则优先于临时偏好。

## 当前项目状态

- 当前阶段：项目初始化与计划制定。
- 当前要求：暂时不要实现业务代码。
- 完整需求来源：`docs/PROJECT_REQUIREMENTS.md`。
- 执行计划来源：`docs/IMPLEMENTATION_PLAN.md`。
- 验收清单来源：`docs/CHECKLIST.md`。

## 每次开发前必须阅读

每次开始任务前，必须按顺序阅读：

1. `AGENTS.md`
2. `docs/PROJECT_REQUIREMENTS.md`
3. `docs/IMPLEMENTATION_PLAN.md`
4. `docs/CHECKLIST.md`

如果需求、计划、清单之间存在冲突，必须先指出冲突并等待用户确认，不能直接按猜测实现。

## 工作边界

- 在用户补充完整需求并确认进入实现阶段前，不得编写业务代码。
- 不得自行引入框架、数据库、云服务、第三方 API 或模型服务，除非需求或用户明确指定。
- 不得把推测当成需求。所有不确定内容必须标记为“待确认”。
- 不得做与当前阶段无关的重构、优化或目录调整。
- 不得删除用户已有文件或改动未授权内容。

## 开发流程

1. 先确认当前任务属于 `docs/IMPLEMENTATION_PLAN.md` 中的哪个阶段。
2. 开工前说明本次要改哪些文件、为什么改。
3. 只完成当前阶段必要内容。
4. 每完成一个阶段，更新 `docs/CHECKLIST.md` 中对应验收项状态。
5. 如果需求发生变化，同步更新 `docs/PROJECT_REQUIREMENTS.md`、`docs/IMPLEMENTATION_PLAN.md` 和 `docs/CHECKLIST.md`。
6. 业务代码实现完成后，必须运行与变更范围匹配的检查或测试；无法运行时必须说明原因。

## 文档规范

- 文档使用中文编写。
- 每个需求应尽量包含：目标、用户场景、输入、输出、异常情况、验收标准。
- 执行计划必须拆成可验证的小阶段，避免一次性大改。
- 验收清单必须能被逐项检查，不写泛泛而谈的目标。

## 代码规范

以下规则从真正进入实现阶段后生效：

- 优先沿用项目已有技术栈和目录结构。
- 保持模块职责清晰，避免把 UI、业务逻辑、数据访问、外部服务调用混在一起。
- 对音频处理、转写、文件存储、任务状态等核心流程保留清晰边界。
- 涉及用户文件、密钥、接口令牌、音频内容和转写文本时，必须按隐私敏感数据处理。
- 错误处理必须给出可理解的失败原因，不能只吞掉异常。
- 新增配置必须提供示例或说明，不能依赖只有本机存在的隐式环境。

## 提交前检查

进入实现阶段后，每次交付前至少确认：

- 当前阶段验收项已更新。
- 没有实现未确认需求。
- 没有引入无说明的外部依赖。
- 没有提交密钥、令牌、真实用户音频或隐私数据。
- 关键路径有必要的测试、手动验证说明或明确的未验证原因。

## 工作流程规则

- 每轮任务开始时，先根据用户需求和项目文档创建清晰的 todo。
- todo 必须包含：读取相关文件、理解现有实现、制定修改步骤、实施修改、验证语法和关键功能、总结改动。
- 修改代码前必须先阅读相关文件，避免凭空改动。
- 优先做局部修改，避免重写整个大文件。
- 修改大文件时必须分段处理，避免一次性覆盖造成内容丢失。
- 每完成一个阶段，更新 docs/CHECKLIST.md 或在最终回复中说明完成项。
- 修改完成后必须说明：改了哪些文件、完成了哪些需求、如何验证、还有哪些风险。
- 不得保存、打印或回传用户 API Key。
- 不得擅自删减 docs/PROJECT_REQUIREMENTS.md 中的需求。