# 项目验收清单

本文档根据 `docs/PROJECT_REQUIREMENTS.md` 的真实需求整理。状态标记如下：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞、待确认或需要阶段开始前决策

## 阶段 0：需求冻结与计划更新

- [x] 完整项目需求已写入 `docs/PROJECT_REQUIREMENTS.md`。
- [x] 项目形态已调整：同一项目以本地 zip / 桌面启动运行为主。
- [x] 后端技术栈已确认：Python FastAPI。
- [x] 运行形态已确认：Windows / Mac 本地启动脚本 + 自动环境准备。
- [x] 数据策略已确认：无登录、无服务端数据库，历史和配置保存在浏览器 IndexedDB。
- [x] 核心处理链路已确认：B站输入解析、字幕处理、音频处理、两段模型处理、Markdown 输出。
- [x] MVP 优先级已记录到实施计划。
- [x] `docs/IMPLEMENTATION_PLAN.md` 已根据真实需求重写。
- [x] `docs/CHECKLIST.md` 已根据真实需求重写。
- [x] 阶段 1 开工前前端形态已确认：原生 HTML/CSS/JS。

## 阶段 1：工程骨架基础

- [x] 根目录包含 `README.md`。
- [x] 根目录包含后端代码目录。
- [x] 根目录包含前端代码目录。
- [x] 根目录包含依赖文件。
- [x] 根目录包含示例环境变量说明。
- [x] 后端目录结构支持 `main.py`、`models.py`、`task_manager.py`、`bilibili.py`、`subtitle.py`、`audio.py`、`llm_client.py`、`prompts.py`、`utils.py`、`cleanup.py` 等模块。
- [x] 前端目录结构支持 `main.js`、`api.js`、`db.js`、`state.js`、`components/`、`styles.css` 或等价模块。
- [x] FastAPI 服务可以启动。
- [x] `GET /api/health` 可用。
- [x] 前端可以打开基础页面。
- [x] 前端可以请求后端健康检查。
- [x] 阶段 1 已按当时的 `docs/IMPLEMENTATION_PLAN.md` 复核：目录结构、FastAPI 应用导入、健康检查和前端首页通过当时验证。

## 本轮清理：远程运行痕迹移除

- [x] Render 专用文件 `render.yaml` 已删除。
- [x] Docker 专用文件 `Dockerfile` 和 `.dockerignore` 已删除。
- [x] README 已移除 Render、Docker、Deploy to Render、免费层和冷启动说明。
- [x] 项目文档已移除远程运行历史参考，保留 Windows / Mac 本地桌面 zip 启动版主线。
- [x] 阶段 4.1 下一步仍为本地桌面启动版与自动环境准备。
- [x] 阶段 4.2 下一步仍为本地 B站高级访问模式，之后再进入阶段 5。

## 阶段 2：前端基础 UI、IndexedDB 与模型配置

- [x] 页面顶部标题为“B站视频转文字”。
- [x] 主输入区支持粘贴完整分享文本、普通 B站网址或 BV 号。
- [x] 页面包含开始按钮。
- [x] 页面包含任务进度条。
- [x] 页面包含当前阶段提示。
- [x] 页面包含类似控制台的完整日志面板。
- [x] 日志面板使用等宽字体。
- [x] 页面包含最终 Markdown 文稿展示区。
- [x] 页面包含中间结果折叠区。
- [x] 页面包含历史记录区域或历史记录页面。
- [x] 页面包含设置区域或设置弹窗。
- [x] 前端视觉风格干净、简洁、低干扰。
- [x] 桌面端布局清晰。
- [x] 移动端有基础适配。
- [x] 前端 API 调用封装在 `api.js` 或等价模块中。
- [x] IndexedDB 操作封装在 `db.js` 或等价模块中。
- [x] IndexedDB 保存第一个模型配置。
- [x] IndexedDB 保存第二个模型配置。
- [x] 模型配置字段包含 API Base URL、API Key、Model、Temperature、Stream。
- [x] Stream 默认开启。
- [x] 每个模型区有“获取模型列表”按钮。
- [x] 获取模型列表通过 `POST /api/models/list` 调用后端。
- [x] 获取模型列表成功后，用户可以选择模型名填入配置。
- [x] API Key 刷新页面后仍可从 IndexedDB 恢复。
- [x] 音频文件不会保存到 IndexedDB。
- [x] 阶段 2 已按 `docs/IMPLEMENTATION_PLAN.md` 复核：前端基础 UI、IndexedDB 配置与历史数据结构、模型列表代理调用入口、桌面与移动端基础布局已完成。
- [x] 阶段 2 UI 已按用户反馈修正：首屏为全屏标题和居中输入框，任务状态与最终结果在识别输入后再渐进展示。
- [x] 设置入口已调整为右上角设置图标，设置抽屉先显示“模型配置”和“历史记录”列表，再进入对应二级页面。
- [x] 前端静态资源路径已改为相对路径，兼容 FastAPI 静态托管与 VS Code Live Server 打开方式。
- [x] 本地 Live Server 跨端口访问 FastAPI 已通过开发期 CORS 配置支持。
- [x] 设置抽屉默认隐藏和关闭逻辑已修正，`hidden` 状态不会再被组件 display 样式覆盖。
- [x] 设置抽屉按钮逻辑已统一：主列表页显示关闭，二级页显示返回设置列表和关闭。
- [x] 首页输入框空状态不再显示“等待输入”文案。
- [x] 设置抽屉标题栏已统一：标题居中、无标题下方细线、右上角固定关闭、二级页左上角固定返回。
- [x] 首页输入框已改为单行输入，发送按钮仅在输入内容后淡入显示。
- [x] 模型配置页已调整为标题同行保存按钮、Model 输入框内联模型选择、获取模型按钮与 Model 输入框同排。

## 阶段 3：任务 API、状态机、日志与资源控制

- [x] `POST /api/tasks` 可以创建任务并返回 `task_id`。
- [x] `GET /api/tasks/{task_id}` 可以返回任务状态、进度、阶段、日志、错误和结果。
- [x] `POST /api/tasks/{task_id}/cancel` 可以取消任务。
- [x] `POST /api/models/list` 可以根据 API Base URL 和 API Key 获取模型列表。
- [x] 任务状态包含 `pending`。
- [x] 任务状态包含 `running`。
- [x] 任务状态包含 `failed`。
- [x] 任务状态包含 `completed`。
- [x] 任务状态包含 `canceled`。
- [x] 任务阶段包含解析用户输入。
- [x] 任务阶段包含获取视频信息。
- [x] 任务阶段包含获取 B站字幕。
- [x] 任务阶段包含清理 B站字幕。
- [x] 任务阶段包含下载音频。
- [x] 任务阶段包含转换 MP3。
- [x] 任务阶段包含音频切片。
- [x] 任务阶段包含音频切片转文字。
- [x] 任务阶段包含合并 AI 音频转文字稿。
- [x] 任务阶段包含文稿优化。
- [x] 任务阶段包含生成最终 Markdown。
- [x] 任务阶段包含清理临时文件。
- [x] 任务阶段包含完成。
- [x] 进度条根据阶段更新。
- [x] 前端可以通过轮询读取任务状态。
- [x] 前端可以实时刷新日志面板。
- [x] 前端可以取消任务。
- [x] 日志支持普通信息、警告和错误。
- [x] 日志统一脱敏 API Key、Authorization 头和 Bearer token。
- [x] 后端不把 API Key 写入日志。
- [x] 后端不把 API Key 保存到文件。
- [x] 后端不把 API Key 回传给前端。
- [x] 临时目录按 `task_id` 创建。
- [x] 每个任务有独立临时目录。
- [x] 任务完成后清理临时目录。
- [x] 任务失败后清理临时目录。
- [x] 任务取消后清理临时目录。
- [x] 全局重任务并发默认上限为 1。
- [x] 单任务音频片段转写并发默认上限为 2。
- [x] 长时间无前端轮询的任务有 abandoned 标记或清理入口。
- [x] 阶段 3 已按 `docs/IMPLEMENTATION_PLAN.md` 复核：任务 API、状态机、mock 状态流、统一日志脱敏、临时目录隔离与清理、资源并发限制、前端创建/轮询/取消闭环均已完成。

## 阶段 4：B站输入解析、视频信息与字幕处理

- [x] 可以从普通 B站视频网址中提取视频标识。
- [x] 可以从完整分享文本中提取视频网址或 BV 号。
- [x] 可以从 BV 号中提取视频标识。
- [x] 给定示例分享文本可以正确解析出 `BV1NvRyBzEhq` 或对应网址。
- [~] 后端可以获取视频标题；已接入 yt-dlp，当前本机真实请求示例 BV 时被 B站返回 HTTP 412，需要在可访问网络环境复测。
- [~] 后端可以获取 BV 号；已完成归一化结构，真实 B站返回同受 HTTP 412 影响需复测。
- [~] 后端可以获取 P 序号；已完成 URL `p` 参数和 yt-dlp `page` 字段处理，真实 B站返回同受 HTTP 412 影响需复测。
- [~] 后端可以获取分P列表；已预留 `entries` 和 `pages` 两类结构处理，真实 B站返回同受 HTTP 412 影响需复测。
- [~] 后端可以获取视频时长；已完成字段归一化，真实 B站返回同受 HTTP 412 影响需复测。
- [~] 单视频可以直接处理；任务流已接入，真实 B站信息获取受 HTTP 412 影响需复测。
- [~] 当前分P可以处理；URL `p` 参数、当前分P选择和结果结构已接入，真实 B站信息获取受 HTTP 412 影响需复测。
- [x] 识别到多P或多视频时，数据结构预留子任务。
- [x] 多P或多视频不会被合并成一份最终文稿。
- [~] 后端会尽量获取 B站自带字幕；字幕候选选择和下载已实现，真实字幕获取需等待视频信息请求可用后复测。
- [~] 字幕来源支持 UP 主上传字幕；代码已支持 `subtitles` 来源，需真实视频字幕样例复测。
- [~] 字幕来源支持 B站自动字幕；代码已支持 `automatic_captions` 来源，需真实视频字幕样例复测。
- [x] 多语言字幕默认优先中文。
- [x] 没有中文字幕时使用英文。
- [x] WEBVTT 清理会删除 `WEBVTT` 头。
- [x] WEBVTT 清理会删除时间轴。
- [x] WEBVTT 清理会删除纯数字序号。
- [x] WEBVTT 清理会删除空行。
- [x] WEBVTT 清理会删除 NOTE、STYLE、REGION 等元信息。
- [x] WEBVTT 清理会保留字幕正文顺序。
- [x] WEBVTT 清理不会擅自改写字幕正文。
- [x] JSON 字幕可以抽取正文并按时间排序。
- [x] 字幕失败时区分没有检测到字幕。
- [x] 字幕失败时区分获取字幕超时。
- [x] 字幕失败时区分字幕格式无法解析。
- [x] 字幕失败时区分 B站返回异常。
- [x] 字幕失败时区分 yt-dlp 或内部技术错误。
- [x] 字幕失败时前端弹出明确提示。
- [x] 字幕失败时用户可以选择跳过字幕继续。

阶段 4 当前说明：阶段 4 代码实现、离线解析/清理测试、任务层模拟测试和前端弹窗交互测试已完成；真实 B站元数据请求在本机对真实公开视频返回 HTTP 412 Precondition Failed，已归一化为明确错误。后续又验证了 `yt-dlp --impersonate chrome` 仍返回 HTTP 412，`yt-dlp --cookies-from-browser chrome` 返回 `Could not copy Chrome cookie database`。因此当前路线为本地桌面启动版和本地 B站高级访问模式。

## 阶段 4.1：本地桌面启动版与自动环境准备

- [ ] 阶段 4.1 未提前实现音频下载、MP3 转换、音频切片、模型转写或最终 Markdown 生成。
- [ ] 已设计本地 zip 目录结构。
- [ ] 已创建 Windows 启动脚本，例如 `start-windows.bat` 或等价脚本。
- [ ] 已创建 Mac 启动脚本，例如 `start-mac.command` 或等价脚本。
- [ ] 启动脚本会检测本机是否已有可用 Python。
- [ ] 检测到已有 Python 时，不强制安装固定小版本。
- [ ] 检测不到 Python 时，优先下载 Python 3.12 系列运行时。
- [ ] Python 下载来源优先使用官方 CPython GitHub Releases 或 Python 官方下载地址。
- [ ] 实现阶段已验证 Windows 可用 Python 官方二进制包下载路径。
- [ ] 实现阶段已验证 Mac 可用 Python 官方二进制包下载路径。
- [ ] 启动脚本会检测 FFmpeg 是否可用。
- [ ] 检测不到 FFmpeg 时，会自动下载并解压 FFmpeg。
- [ ] FFmpeg 会放到项目可控目录或用户本地应用数据目录。
- [ ] 启动脚本会创建或复用本地虚拟环境。
- [ ] 启动脚本会安装后端依赖。
- [ ] 启动脚本会启动 FastAPI 服务。
- [ ] 启动脚本会自动打开默认浏览器访问 `http://127.0.0.1:8000`。
- [ ] 如果 8000 端口被占用，脚本会自动选择下一个可用端口。
- [ ] 如果使用备用端口，脚本会打开对应 URL。
- [ ] 控制台会显示检测 Python 的状态。
- [ ] 控制台会显示检测 FFmpeg 的状态。
- [ ] 控制台会显示安装依赖的状态。
- [ ] 控制台会显示启动服务的状态。
- [ ] 控制台会显示打开浏览器的状态。
- [ ] 启动失败时会给出明确错误和下一步建议。
- [ ] 本地临时音频、字幕、日志和依赖缓存有清楚目录边界。
- [ ] README 增加下载 zip 本地运行说明。
- [ ] README 面向普通用户说明双击启动流程。
- [ ] README 包含 Windows 本地启动常见问题。
- [ ] README 包含 Mac 本地启动常见问题。

## 阶段 4.2：本地 B站高级访问模式

- [ ] 阶段 4.2 未提前实现音频下载、MP3 转换、音频切片、模型转写或最终 Markdown 生成。
- [ ] 阶段 4.2 只服务本地桌面运行版。
- [ ] 已记录普通 yt-dlp 匿名请求真实公开视频返回 HTTP 412 的验证结果。
- [ ] 已记录 `yt-dlp --impersonate chrome` 真实公开视频返回 HTTP 412 的验证结果。
- [ ] 已记录 `yt-dlp --cookies-from-browser chrome` 返回 `Could not copy Chrome cookie database` 的验证结果。
- [ ] 后端建立本地访问策略枚举，例如 `anonymous`、`impersonate`、`browser_cookie`、`cookies_file`、`local_browser_profile`。
- [ ] 标准匿名请求和增强请求头保留为第一层尝试。
- [ ] `curl_cffi` / `--impersonate chrome` 保留为轻量尝试路径。
- [ ] 本地高级模式支持用户明确选择浏览器 Cookie 来源。
- [ ] 浏览器 Cookie 来源至少评估 Chrome。
- [ ] 浏览器 Cookie 来源至少评估 Edge。
- [ ] 浏览器 Cookie 来源至少评估 Firefox。
- [ ] `cookies-from-browser` 失败时，前端提示浏览器 Cookie 数据库复制失败及可尝试动作。
- [ ] 已评估或实现 `cookies.txt` 导入作为高级用户兜底能力。
- [ ] 如实现 `cookies.txt` 导入，Cookie 内容不写入日志。
- [ ] 如实现 `cookies.txt` 导入，Cookie 内容不保存到 IndexedDB。
- [ ] 如实现 `cookies.txt` 导入，Cookie 内容不上传到第三方服务。
- [ ] 已评估本地专用浏览器登录窗口或本地持久浏览器 Profile 方案。
- [ ] 如引入 Playwright、Puppeteer、Chromium 等能力，仅用于本地版。
- [ ] 如引入本地浏览器 Profile 方案，登录态只保存在用户本机可控目录。
- [ ] 前端提供“B站访问模式”或等价设置入口。
- [ ] 默认访问模式为自动模式。
- [ ] 高级访问选项默认折叠隐藏。
- [ ] HTTP 412 会被识别为 B站风控或访问前置条件失败，而不是内部错误。
- [ ] HTTP 403 会被识别为访问被拒绝或权限受限。
- [ ] HTTP 404 会被识别为视频不存在或无法访问。
- [ ] B站访问超时会返回明确错误。
- [ ] yt-dlp 内部错误会返回明确错误。
- [ ] Cookie 无效会返回明确错误。
- [ ] 任务日志会记录当前使用的访问策略。
- [ ] 任务日志不会记录 Cookie、token、敏感请求头或敏感本机路径。
- [ ] 字幕失败弹窗仍支持跳过字幕继续。
- [ ] README 增加本地 B站访问排错说明。

## 阶段 5：音频下载、MP3 转换与切片

- [ ] 后端只下载音频，不下载完整视频。
- [ ] 下载文件名使用安全文件名。
- [ ] 视频需要登录权限时返回 MVP 暂不支持的明确错误。
- [ ] 付费或受限内容返回 MVP 暂不支持的明确错误。
- [ ] 音频下载失败会写入日志。
- [ ] FFmpeg 不可用时返回明确错误。
- [ ] 音频可以统一转换成 MP3。
- [ ] MP3 转换失败时返回明确错误。
- [ ] 短于或等于 20 分钟的音频不切片。
- [ ] 超过 20 分钟的音频会切片。
- [ ] 切片目标长度约 20 分钟。
- [ ] 切片优先在 18 到 22 分钟范围内寻找静音位置。
- [ ] 静音检测使用 FFmpeg silencedetect 或等价方案。
- [ ] 找不到合适静音点时按 20 分钟硬切。
- [ ] 切片文件按 `part_001.mp3`、`part_002.mp3` 等编号。
- [ ] 日志记录切片数量。
- [ ] 日志记录每段时长。
- [ ] 日志记录切分位置。
- [ ] 音频切片失败时返回明确错误。

## 阶段 6：第一模型音频转文字

- [ ] 音频转文字模型调用封装为独立模块。
- [ ] 模型调用使用用户配置的 API Base URL。
- [ ] 模型调用使用用户配置的 API Key。
- [ ] 模型调用使用用户配置的 Model。
- [ ] 模型调用使用用户配置的 Temperature。
- [ ] 模型调用使用用户配置的 Stream。
- [ ] 输入音频必须为 MP3。
- [ ] MP3 以 `input_audio` / base64 mp3 方案构造 payload。
- [ ] payload 构造放在独立函数中。
- [ ] 音频转文字提示词已作为常量保存。
- [ ] 每个音频切片按编号转写。
- [ ] 第一个音频片段开始后，每隔 10 秒启动下一个片段请求。
- [ ] 单任务片段请求并发上限默认 2。
- [ ] 每个音频片段最多自动重试 3 次。
- [ ] API 超时会记录明确错误。
- [ ] API 限流会记录明确错误。
- [ ] 格式错误会记录明确错误。
- [ ] 模型不支持音频多模态会记录明确错误。
- [ ] 鉴权失败会记录明确错误。
- [ ] 网络错误会记录明确错误。
- [ ] 模型返回空内容会记录明确错误。
- [ ] 某段 3 次都失败时任务失败。
- [ ] 前端显示失败片段的错误原因。
- [ ] 所有切片成功后按编号合并。
- [ ] 合并结果形成完整 AI 音频转文字稿。
- [ ] 合并时只做简单去重和段落修整。
- [ ] 不调用额外模型清理每段转写结果。
- [ ] 前端显示当前切片进度。

## 阶段 7：第二模型文稿优化与 Markdown 输出

- [ ] 第二模型内置提示词原样保存到 `prompts.py` 或等价常量文件。
- [ ] 第二模型调用使用 OpenAI-compatible Chat Completions。
- [ ] 第二模型输入包含 `<YourTask>`。
- [ ] 第二模型输入包含 `<OriginalSubtitleContent>`。
- [ ] 第二模型输入包含 `<AIAudioTranscriptionResult>`。
- [ ] 无字幕但用户选择继续时，字幕输入使用需求指定占位文本。
- [ ] 第二模型使用用户配置的 API Base URL。
- [ ] 第二模型使用用户配置的 API Key。
- [ ] 第二模型使用用户配置的 Model。
- [ ] 第二模型使用用户配置的 Temperature。
- [ ] 第二模型使用用户配置的 Stream。
- [ ] 系统可以识别并清理 `<think>...</think>` 内容。
- [ ] 如果存在 `</think>`，取最后一个 `</think>` 之后的内容。
- [ ] 删除残留 `<think>` 和 `</think>` 标签。
- [ ] 删除输出中的 `KINGFALL MODE ENABLE` 字样。
- [ ] 最终展示内容为干净 Markdown 正文。
- [ ] 第二模型输出格式异常时返回明确错误。
- [ ] 最终文件名格式为 `P序号_视频标题_日期时间.md`。
- [ ] 文件名会清理非法字符。
- [ ] 中文标题可以保留。
- [ ] 日期时间使用浏览器本地时间生成。
- [ ] 前端可以展示最终 Markdown。
- [ ] 前端可以下载最终 `.md` 文件。
- [ ] 前端可以展开查看清理后的 B站字幕。
- [ ] 前端可以展开查看 AI 音频转文字稿。
- [ ] 前端可以展开查看完整日志。

## 阶段 8：历史记录、导入导出与多P基础支持

- [ ] IndexedDB 保存历史任务列表。
- [ ] 每个历史任务保存视频标题。
- [ ] 每个历史任务保存原始输入。
- [ ] 每个历史任务保存解析后网址或 BV 号。
- [ ] 每个历史任务保存创建时间。
- [ ] 每个历史任务保存最终 Markdown 文稿。
- [ ] 每个历史任务保存清理后 B站字幕文本。
- [ ] 每个历史任务保存 AI 音频转文字稿。
- [ ] 每个历史任务保存完整日志。
- [ ] 每个历史任务保存错误信息。
- [ ] 多P任务保存每个子任务结果。
- [ ] 历史记录按时间由新到旧展示。
- [ ] 历史搜索支持标题。
- [ ] 历史搜索支持正文。
- [ ] 支持全量导出配置和历史结果为 JSON。
- [ ] 支持从 JSON 全量导入配置和历史结果。
- [ ] 支持清空历史记录。
- [ ] 历史记录不需要标签。
- [ ] 历史记录不需要收藏。
- [ ] 多P任务中每个视频或分P都有独立 Markdown 文件。
- [ ] 页面按视频或分P折叠展示结果。
- [ ] 中间结果高级区域可下载清理后的字幕 TXT。
- [ ] 中间结果高级区域可下载 AI 音频转文字 TXT。
- [ ] 中间结果高级区域可下载日志 TXT。

## 阶段 9：错误处理、安全隐私、文档与发布验收

- [ ] 用户输入无法解析时返回明确错误。
- [ ] B站视频不存在或无法访问时返回明确错误。
- [ ] 视频需要登录权限时返回明确错误。
- [ ] 没有检测到字幕时返回明确错误。
- [ ] 字幕获取超时时返回明确错误。
- [ ] 字幕格式无法解析时返回明确错误。
- [ ] 音频下载失败时返回明确错误。
- [ ] FFmpeg 不可用时返回明确错误。
- [ ] MP3 转换失败时返回明确错误。
- [ ] 音频切片失败时返回明确错误。
- [ ] 模型 API Base URL 无效时返回明确错误。
- [ ] API Key 无效时返回明确错误。
- [ ] 模型列表获取失败时返回明确错误。
- [ ] 模型不支持音频多模态时返回明确错误。
- [ ] API 超时时返回明确错误。
- [ ] API 限流时返回明确错误。
- [ ] 模型返回空内容时返回明确错误。
- [ ] 第二模型输出格式异常时返回明确错误。
- [ ] 临时文件清理失败时返回明确错误或警告。
- [ ] 错误显示在当前任务状态中。
- [ ] 错误显示在日志面板中。
- [ ] 错误显示在任务详情中。
- [ ] 日志中不出现完整 API Key。
- [ ] 日志中不出现完整 Authorization 头。
- [ ] 日志中不出现完整 Bearer token。
- [ ] 后端只临时处理当前任务音频和文本。
- [ ] 任务完成或失败后清理临时文件。
- [ ] README 包含项目介绍。
- [ ] README 包含功能说明。
- [ ] README 包含本地运行方法。
- [ ] README 包含 Windows 本地启动方法。
- [ ] README 包含 Mac 本地启动方法。
- [ ] README 包含本地 B站访问排错说明。
- [ ] README 包含环境变量说明。
- [ ] README 包含常见错误排查。
- [ ] README 包含数据隐私说明。
- [ ] README 包含使用边界说明。
- [ ] README 说明如何下载项目 zip。
- [ ] README 说明启动脚本会检测 Python、FFmpeg、依赖和端口。
- [ ] README 说明 HTTP 412 或 Cookie 读取失败时的含义和可尝试动作。
- [ ] README 说明后端本地文件是临时的。
- [ ] README 说明历史记录和模型配置保存在 IndexedDB。
- [ ] README 说明迁移浏览器或电脑需要先导出再导入。
- [ ] README 说明长视频或大量任务需要预留足够本机磁盘、CPU、内存和网络时间。

## MVP 总体验收

- [x] 前端可以打开。
- [x] 前端可以保存两个模型配置到 IndexedDB。
- [x] 前端可以从完整分享文本中提取 B站网址或 BV 号。
- [~] 后端可以获取 B站视频信息；代码已接入 yt-dlp，当前本机真实请求被 B站 HTTP 412 拦截，需复测。
- [ ] 本地 zip 启动版可用，普通用户可以通过脚本自动准备环境并打开本地网页。
- [ ] 本地 B站高级访问模式可用，能对 HTTP 412、Cookie 读取失败等情况给出明确路径。
- [~] 后端可以尝试获取字幕，并清理 VTT 字幕为纯文本；清理已离线验证，真实字幕获取需在视频信息请求可用后复测。
- [x] 字幕失败时能返回明确错误，并允许用户跳过字幕继续。
- [ ] 后端可以下载音频并转换 MP3。
- [ ] 后端可以对长音频切片。
- [ ] 后端可以调用第一个模型进行 MP3 音频转文字。
- [ ] 后端可以合并多段音频转写结果。
- [ ] 后端可以调用第二个模型生成最终 Markdown 文稿。
- [ ] 系统可以清理 `<think>...</think>` 内容，只展示干净正文。
- [ ] 前端可以展示最终 Markdown。
- [ ] 前端可以下载最终 `.md` 文件。
- [ ] 前端可以展开中间结果。
- [ ] 前端可以查看完整日志。
- [ ] 前端可以保存历史记录。
- [ ] 前端可以搜索历史记录。
- [x] 前端可以导出和导入 IndexedDB 数据。
- [ ] 后端任务完成或失败后会清理临时文件。
- [ ] 日志不会泄露 API Key、Cookie、浏览器登录态或其他敏感信息。

## 当前下一步

- [x] GitHub 仓库 `Naroahgany/Video2text` 已创建并推送初始代码。
- [x] 阶段 1 已完成，可进入阶段 2。
- [x] 进入阶段 2：前端基础 UI、IndexedDB 与模型配置。
- [x] 阶段 2 已完成，可进入阶段 3。
- [x] 进入阶段 3：任务 API、状态机、日志与资源控制。
- [x] 阶段 3 已完成，可进入阶段 4。
- [x] 进入阶段 4：B站输入解析、视频信息与字幕处理。
- [~] 阶段 4 已完成代码接入和本地离线/模拟/前端验证；真实 B站视频信息与字幕获取因当前网络返回 HTTP 412 需要复测。
- [ ] 下一阶段为阶段 4.1：本地桌面启动版与自动环境准备；不得提前实现阶段 5 及后续音频、模型链路。
- [ ] 阶段 4.1 完成后进入阶段 4.2：本地 B站高级访问模式。
- [ ] 阶段 4.2 完成后再进入阶段 5：音频下载、MP3 转换与切片。
