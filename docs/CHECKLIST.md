# 项目验收清单

本文档根据 `docs/PROJECT_REQUIREMENTS.md` 的真实需求整理。状态标记如下：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞、待确认或需要阶段开始前决策

## 阶段 0：需求冻结与计划更新

- [x] 完整项目需求已写入 `docs/PROJECT_REQUIREMENTS.md`。
- [x] 项目形态已确认：可部署到 Render 的 B站视频转文字 Web 项目。
- [x] 后端技术栈已确认：Python FastAPI。
- [x] 部署形态已确认：Docker + `render.yaml` + README Deploy to Render。
- [x] 数据策略已确认：无登录、无服务端数据库，历史和配置保存在浏览器 IndexedDB。
- [x] 核心处理链路已确认：B站输入解析、字幕处理、音频处理、两段模型处理、Markdown 输出。
- [x] MVP 优先级已记录到实施计划。
- [x] `docs/IMPLEMENTATION_PLAN.md` 已根据真实需求重写。
- [x] `docs/CHECKLIST.md` 已根据真实需求重写。
- [x] 阶段 1 开工前前端形态已确认：原生 HTML/CSS/JS。

## 阶段 1：工程骨架与部署基础

- [x] 根目录包含 `Dockerfile`。
- [x] 根目录包含 `render.yaml`。
- [x] 根目录包含 `README.md`。
- [x] 根目录包含后端代码目录。
- [x] 根目录包含前端代码目录。
- [x] 根目录包含依赖文件。
- [x] 根目录包含示例环境变量说明。
- [x] 后端目录结构支持 `main.py`、`models.py`、`task_manager.py`、`bilibili.py`、`subtitle.py`、`audio.py`、`llm_client.py`、`prompts.py`、`utils.py`、`cleanup.py` 等模块。
- [x] 前端目录结构支持 `main.js`、`api.js`、`db.js`、`state.js`、`components/`、`styles.css` 或等价模块。
- [x] FastAPI 服务可以启动。
- [x] `GET /api/health` 可用。
- [x] 前端可以打开基础页面。
- [x] 前端可以请求后端健康检查。
- [x] Docker 镜像配置安装 FFmpeg。
- [x] Docker 镜像配置安装 yt-dlp。
- [x] 本地 Docker 可以成功启动项目。
- [x] Render 部署文件结构完整。
- [x] 阶段 1 已按 `docs/IMPLEMENTATION_PLAN.md` 复核：目录结构、FastAPI 应用导入、健康检查、前端首页、已有 Docker 镜像运行、FFmpeg、yt-dlp 和 Render 部署结构通过当前验证。
- [x] 阶段 1 判定完成；重新执行 `docker build` 时遇到 Debian 源网络 502 或超时属于环境网络问题，不作为阶段 1 阻塞项，后续需要重建镜像时可使用 README 中的镜像源构建命令。

## 阶段 2：前端基础 UI、IndexedDB 与模型配置

- [ ] 页面顶部标题为“B站视频转文字”。
- [ ] 主输入区支持粘贴完整分享文本、普通 B站网址或 BV 号。
- [ ] 页面包含开始按钮。
- [ ] 页面包含任务进度条。
- [ ] 页面包含当前阶段提示。
- [ ] 页面包含类似控制台的完整日志面板。
- [ ] 日志面板使用等宽字体。
- [ ] 页面包含最终 Markdown 文稿展示区。
- [ ] 页面包含中间结果折叠区。
- [ ] 页面包含历史记录区域或历史记录页面。
- [ ] 页面包含设置区域或设置弹窗。
- [ ] 前端视觉风格干净、简洁、低干扰。
- [ ] 桌面端布局清晰。
- [ ] 移动端有基础适配。
- [ ] 前端 API 调用封装在 `api.js` 或等价模块中。
- [ ] IndexedDB 操作封装在 `db.js` 或等价模块中。
- [ ] IndexedDB 保存第一个模型配置。
- [ ] IndexedDB 保存第二个模型配置。
- [ ] 模型配置字段包含 API Base URL、API Key、Model、Temperature、Stream。
- [ ] Stream 默认开启。
- [ ] 每个模型区有“获取模型列表”按钮。
- [ ] 获取模型列表通过 `POST /api/models/list` 调用后端。
- [ ] 获取模型列表成功后，用户可以选择模型名填入配置。
- [ ] API Key 刷新页面后仍可从 IndexedDB 恢复。
- [ ] 音频文件不会保存到 IndexedDB。

## 阶段 3：任务 API、状态机、日志与资源控制

- [ ] `POST /api/tasks` 可以创建任务并返回 `task_id`。
- [ ] `GET /api/tasks/{task_id}` 可以返回任务状态、进度、阶段、日志、错误和结果。
- [ ] `POST /api/tasks/{task_id}/cancel` 可以取消任务。
- [ ] `POST /api/models/list` 可以根据 API Base URL 和 API Key 获取模型列表。
- [ ] 任务状态包含 `pending`。
- [ ] 任务状态包含 `running`。
- [ ] 任务状态包含 `failed`。
- [ ] 任务状态包含 `completed`。
- [ ] 任务状态包含 `canceled`。
- [ ] 任务阶段包含解析用户输入。
- [ ] 任务阶段包含获取视频信息。
- [ ] 任务阶段包含获取 B站字幕。
- [ ] 任务阶段包含清理 B站字幕。
- [ ] 任务阶段包含下载音频。
- [ ] 任务阶段包含转换 MP3。
- [ ] 任务阶段包含音频切片。
- [ ] 任务阶段包含音频切片转文字。
- [ ] 任务阶段包含合并 AI 音频转文字稿。
- [ ] 任务阶段包含文稿优化。
- [ ] 任务阶段包含生成最终 Markdown。
- [ ] 任务阶段包含清理临时文件。
- [ ] 任务阶段包含完成。
- [ ] 进度条根据阶段更新。
- [ ] 前端可以通过轮询读取任务状态。
- [ ] 前端可以实时刷新日志面板。
- [ ] 前端可以取消任务。
- [ ] 日志支持普通信息、警告和错误。
- [ ] 日志统一脱敏 API Key、Authorization 头和 Bearer token。
- [ ] 后端不把 API Key 写入日志。
- [ ] 后端不把 API Key 保存到文件。
- [ ] 后端不把 API Key 回传给前端。
- [ ] 临时目录按 `task_id` 创建。
- [ ] 每个任务有独立临时目录。
- [ ] 任务完成后清理临时目录。
- [ ] 任务失败后清理临时目录。
- [ ] 任务取消后清理临时目录。
- [ ] 全局重任务并发默认上限为 1。
- [ ] 单任务音频片段转写并发默认上限为 2。
- [ ] 长时间无前端轮询的任务有 abandoned 标记或清理入口。

## 阶段 4：B站输入解析、视频信息与字幕处理

- [ ] 可以从普通 B站视频网址中提取视频标识。
- [ ] 可以从完整分享文本中提取视频网址或 BV 号。
- [ ] 可以从 BV 号中提取视频标识。
- [ ] 给定示例分享文本可以正确解析出 `BV1NvRyBzEhq` 或对应网址。
- [ ] 后端可以获取视频标题。
- [ ] 后端可以获取 BV 号。
- [ ] 后端可以获取 P 序号。
- [ ] 后端可以获取分P列表。
- [ ] 后端可以获取视频时长。
- [ ] 单视频可以直接处理。
- [ ] 当前分P可以处理。
- [ ] 识别到多P或多视频时，数据结构预留子任务。
- [ ] 多P或多视频不会被合并成一份最终文稿。
- [ ] 后端会尽量获取 B站自带字幕。
- [ ] 字幕来源支持 UP 主上传字幕。
- [ ] 字幕来源支持 B站自动字幕。
- [ ] 多语言字幕默认优先中文。
- [ ] 没有中文字幕时使用英文。
- [ ] WEBVTT 清理会删除 `WEBVTT` 头。
- [ ] WEBVTT 清理会删除时间轴。
- [ ] WEBVTT 清理会删除纯数字序号。
- [ ] WEBVTT 清理会删除空行。
- [ ] WEBVTT 清理会删除 NOTE、STYLE、REGION 等元信息。
- [ ] WEBVTT 清理会保留字幕正文顺序。
- [ ] WEBVTT 清理不会擅自改写字幕正文。
- [ ] JSON 字幕可以抽取正文并按时间排序。
- [ ] 字幕失败时区分没有检测到字幕。
- [ ] 字幕失败时区分获取字幕超时。
- [ ] 字幕失败时区分字幕格式无法解析。
- [ ] 字幕失败时区分 B站返回异常。
- [ ] 字幕失败时区分 yt-dlp 或内部技术错误。
- [ ] 字幕失败时前端弹出明确提示。
- [ ] 字幕失败时用户可以选择跳过字幕继续。

## 阶段 5：音频下载、MP3 转换与切片

- [ ] 后端只下载音频，不下载完整视频。
- [ ] 下载文件名使用安全文件名。
- [ ] 视频需要登录权限时返回 MVP 暂不支持的明确错误。
- [ ] 付费或受限内容返回 MVP 暂不支持的明确错误。
- [ ] 音频下载失败会写入日志。
- [ ] FFmpeg 不可用时返回明确错误。
- [ ] 音频可以统一转换成 MP3。
- [ ] MP3 转换失败时返回明确错误。
- [ ] 短于或等于 20 分钟的音频不切片。
- [ ] 超过 20 分钟的音频会切片。
- [ ] 切片目标长度约 20 分钟。
- [ ] 切片优先在 18 到 22 分钟范围内寻找静音位置。
- [ ] 静音检测使用 FFmpeg silencedetect 或等价方案。
- [ ] 找不到合适静音点时按 20 分钟硬切。
- [ ] 切片文件按 `part_001.mp3`、`part_002.mp3` 等编号。
- [ ] 日志记录切片数量。
- [ ] 日志记录每段时长。
- [ ] 日志记录切分位置。
- [ ] 音频切片失败时返回明确错误。

## 阶段 6：第一模型音频转文字

- [ ] 音频转文字模型调用封装为独立模块。
- [ ] 模型调用使用用户配置的 API Base URL。
- [ ] 模型调用使用用户配置的 API Key。
- [ ] 模型调用使用用户配置的 Model。
- [ ] 模型调用使用用户配置的 Temperature。
- [ ] 模型调用使用用户配置的 Stream。
- [ ] 输入音频必须为 MP3。
- [ ] MP3 以 `input_audio` / base64 mp3 方案构造 payload。
- [ ] payload 构造放在独立函数中。
- [ ] 音频转文字提示词已作为常量保存。
- [ ] 每个音频切片按编号转写。
- [ ] 第一个音频片段开始后，每隔 10 秒启动下一个片段请求。
- [ ] 单任务片段请求并发上限默认 2。
- [ ] 每个音频片段最多自动重试 3 次。
- [ ] API 超时会记录明确错误。
- [ ] API 限流会记录明确错误。
- [ ] 格式错误会记录明确错误。
- [ ] 模型不支持音频多模态会记录明确错误。
- [ ] 鉴权失败会记录明确错误。
- [ ] 网络错误会记录明确错误。
- [ ] 模型返回空内容会记录明确错误。
- [ ] 某段 3 次都失败时任务失败。
- [ ] 前端显示失败片段的错误原因。
- [ ] 所有切片成功后按编号合并。
- [ ] 合并结果形成完整 AI 音频转文字稿。
- [ ] 合并时只做简单去重和段落修整。
- [ ] 不调用额外模型清理每段转写结果。
- [ ] 前端显示当前切片进度。

## 阶段 7：第二模型文稿优化与 Markdown 输出

- [ ] 第二模型内置提示词原样保存到 `prompts.py` 或等价常量文件。
- [ ] 第二模型调用使用 OpenAI-compatible Chat Completions。
- [ ] 第二模型输入包含 `<YourTask>`。
- [ ] 第二模型输入包含 `<OriginalSubtitleContent>`。
- [ ] 第二模型输入包含 `<AIAudioTranscriptionResult>`。
- [ ] 无字幕但用户选择继续时，字幕输入使用需求指定占位文本。
- [ ] 第二模型使用用户配置的 API Base URL。
- [ ] 第二模型使用用户配置的 API Key。
- [ ] 第二模型使用用户配置的 Model。
- [ ] 第二模型使用用户配置的 Temperature。
- [ ] 第二模型使用用户配置的 Stream。
- [ ] 系统可以识别并清理 `<think>...</think>` 内容。
- [ ] 如果存在 `</think>`，取最后一个 `</think>` 之后的内容。
- [ ] 删除残留 `<think>` 和 `</think>` 标签。
- [ ] 删除输出中的 `KINGFALL MODE ENABLE` 字样。
- [ ] 最终展示内容为干净 Markdown 正文。
- [ ] 第二模型输出格式异常时返回明确错误。
- [ ] 最终文件名格式为 `P序号_视频标题_日期时间.md`。
- [ ] 文件名会清理非法字符。
- [ ] 中文标题可以保留。
- [ ] 日期时间使用浏览器本地时间生成。
- [ ] 前端可以展示最终 Markdown。
- [ ] 前端可以下载最终 `.md` 文件。
- [ ] 前端可以展开查看清理后的 B站字幕。
- [ ] 前端可以展开查看 AI 音频转文字稿。
- [ ] 前端可以展开查看完整日志。

## 阶段 8：历史记录、导入导出与多P基础支持

- [ ] IndexedDB 保存历史任务列表。
- [ ] 每个历史任务保存视频标题。
- [ ] 每个历史任务保存原始输入。
- [ ] 每个历史任务保存解析后网址或 BV 号。
- [ ] 每个历史任务保存创建时间。
- [ ] 每个历史任务保存最终 Markdown 文稿。
- [ ] 每个历史任务保存清理后 B站字幕文本。
- [ ] 每个历史任务保存 AI 音频转文字稿。
- [ ] 每个历史任务保存完整日志。
- [ ] 每个历史任务保存错误信息。
- [ ] 多P任务保存每个子任务结果。
- [ ] 历史记录按时间由新到旧展示。
- [ ] 历史搜索支持标题。
- [ ] 历史搜索支持正文。
- [ ] 支持全量导出配置和历史结果为 JSON。
- [ ] 支持从 JSON 全量导入配置和历史结果。
- [ ] 支持清空历史记录。
- [ ] 历史记录不需要标签。
- [ ] 历史记录不需要收藏。
- [ ] 多P任务中每个视频或分P都有独立 Markdown 文件。
- [ ] 页面按视频或分P折叠展示结果。
- [ ] 中间结果高级区域可下载清理后的字幕 TXT。
- [ ] 中间结果高级区域可下载 AI 音频转文字 TXT。
- [ ] 中间结果高级区域可下载日志 TXT。

## 阶段 9：错误处理、安全隐私、文档与发布验收

- [ ] 用户输入无法解析时返回明确错误。
- [ ] B站视频不存在或无法访问时返回明确错误。
- [ ] 视频需要登录权限时返回明确错误。
- [ ] 没有检测到字幕时返回明确错误。
- [ ] 字幕获取超时时返回明确错误。
- [ ] 字幕格式无法解析时返回明确错误。
- [ ] 音频下载失败时返回明确错误。
- [ ] FFmpeg 不可用时返回明确错误。
- [ ] MP3 转换失败时返回明确错误。
- [ ] 音频切片失败时返回明确错误。
- [ ] 模型 API Base URL 无效时返回明确错误。
- [ ] API Key 无效时返回明确错误。
- [ ] 模型列表获取失败时返回明确错误。
- [ ] 模型不支持音频多模态时返回明确错误。
- [ ] API 超时时返回明确错误。
- [ ] API 限流时返回明确错误。
- [ ] 模型返回空内容时返回明确错误。
- [ ] 第二模型输出格式异常时返回明确错误。
- [ ] 临时文件清理失败时返回明确错误或警告。
- [ ] 错误显示在当前任务状态中。
- [ ] 错误显示在日志面板中。
- [ ] 错误显示在任务详情中。
- [ ] 日志中不出现完整 API Key。
- [ ] 日志中不出现完整 Authorization 头。
- [ ] 日志中不出现完整 Bearer token。
- [ ] 后端只临时处理当前任务音频和文本。
- [ ] 任务完成或失败后清理临时文件。
- [ ] README 包含项目介绍。
- [ ] README 包含功能说明。
- [ ] README 包含本地运行方法。
- [ ] README 包含 Docker 运行方法。
- [ ] README 包含 Render 部署方法。
- [ ] README 包含 Deploy to Render 按钮。
- [ ] README 包含环境变量说明。
- [ ] README 包含常见错误排查。
- [ ] README 包含数据隐私说明。
- [ ] README 包含使用边界说明。
- [ ] README 说明如何 Fork 项目。
- [ ] README 说明 Render 会自动使用 Dockerfile 构建。
- [ ] README 说明免费层级会休眠和冷启动。
- [ ] README 说明后端本地文件是临时的。
- [ ] README 说明历史记录和模型配置保存在 IndexedDB。
- [ ] README 说明迁移浏览器或电脑需要先导出再导入。
- [ ] README 说明长视频或大量任务建议升级部署资源。

## MVP 总体验收

- [x] 本地 Docker 可以成功启动项目。
- [x] Render 部署结构完整。
- [x] 前端可以打开。
- [ ] 前端可以保存两个模型配置到 IndexedDB。
- [ ] 前端可以从完整分享文本中提取 B站网址或 BV 号。
- [ ] 后端可以获取 B站视频信息。
- [ ] 后端可以尝试获取字幕，并清理 VTT 字幕为纯文本。
- [ ] 字幕失败时能返回明确错误，并允许用户跳过字幕继续。
- [ ] 后端可以下载音频并转换 MP3。
- [ ] 后端可以对长音频切片。
- [ ] 后端可以调用第一个模型进行 MP3 音频转文字。
- [ ] 后端可以合并多段音频转写结果。
- [ ] 后端可以调用第二个模型生成最终 Markdown 文稿。
- [ ] 系统可以清理 `<think>...</think>` 内容，只展示干净正文。
- [ ] 前端可以展示最终 Markdown。
- [ ] 前端可以下载最终 `.md` 文件。
- [ ] 前端可以展开中间结果。
- [ ] 前端可以查看完整日志。
- [ ] 前端可以保存历史记录。
- [ ] 前端可以搜索历史记录。
- [ ] 前端可以导出和导入 IndexedDB 数据。
- [ ] 后端任务完成或失败后会清理临时文件。
- [ ] 日志不会泄露 API Key。

## 当前下一步

- [x] 阶段 1 的本地 Docker 启动验证已完成。
- [x] GitHub 仓库 `Naroahgany/Video2text` 已创建并推送初始代码。
- [x] 阶段 1 已完成，可进入阶段 2。
- [ ] 进入阶段 2：前端基础 UI、IndexedDB 与模型配置。
